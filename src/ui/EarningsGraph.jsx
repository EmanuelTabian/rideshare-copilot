import styled from "styled-components";
import {
  Area,
  AreaChart,
  CartesianGrid,
  ResponsiveContainer,
  Tooltip,
  XAxis,
  YAxis,
} from "recharts";

function EarningsGraph() {
  return (
    <>
      <h1>Earnings graph</h1>
      {/* Conditionally rendered when the posts array of objects is empty */}
      {/* <Message>You have no earnings</Message> */}
    </>
  );
}

export default EarningsGraph;
